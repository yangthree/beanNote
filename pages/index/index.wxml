<!-- pages/index/index.wxml -->
<!-- 我的豆单首页：展示个人豆单列表，并提供搜索与新增入口 -->
<view class="page">
  <!-- 顶部标题区域 -->
  <view class="header">
    <view class="header-title">我的豆单</view>
    <view class="header-action">⋮</view>
  </view>

  <!-- 搜索输入框 -->
  <view class="search-container">
    <text class="search-icon">🔍</text>
    <input
      class="search-input"
      placeholder="搜索豆名、品牌、产地或风味..."
      placeholder-style="color:#888888"
      value="{{searchKeyword}}"
      bindinput="onSearchInput"
    />
  </view>

  <!-- 列表区域 -->
  <block wx:if="{{showEmpty}}">
    <!-- 空状态：无记录时给出引导 -->
    <view class="empty-state">
      <text class="empty-title">还没有咖啡豆记录\n</text>
      <text class="empty-hint">点击下方“+”按钮开始记录</text>
    </view>
  </block>
  <block wx:else>
    <view class="bean-list">
      <view
        class="bean-card"
        wx:for="{{coffeeBeans}}"
        wx:key="id"
        bindtap="goToDetail"
        bindlongpress="showDeleteMenu"
        data-id="{{item.id}}"
      >
        <!-- 图片封面：显示豆袋图片或占位 -->
        <view class="bean-image-wrapper">
          <image
            wx:if="{{item.coverImage}}"
            class="bean-image"
            src="{{item.coverImage}}"
            mode="aspectFill"
          />
          <view wx:else class="bean-image-placeholder">☕</view>
          <view class="bean-badge">{{item.type === 'pourOver' ? '手冲' : '意式'}}/{{item.roastLevel}}</view>
        </view>

        <!-- 内容区 -->
        <view class="bean-content">
          <!-- 名称 -->
          <view class="bean-name">{{item.name || '未命名咖啡豆'}}</view>
          <!-- 品牌 + 评分 -->
          <view class="brand-rating-row">
            <view class="bean-brand" wx:if="{{item.brand}}">{{item.brand}}</view>
            <view class="bean-rating">
              <text class="star active">★</text>
              <text class="rating-value">{{item.displayRating}}</text>
            </view>
          </view>
        </view>

        <view class="bean-footer">
          <text class="bean-origin" wx:if="{{item.origin}}">{{item.origin}}</text>
          <text class="bean-date" wx:if="{{item.displayDate}}">{{item.displayDate}}</text>
        </view>
      </view>
    </view>
  </block>

  <!-- 悬浮新增按钮 -->
  <view class="fab" bindtap="goToAdd">
    <text class="fab-icon">＋</text>
  </view>
</view>
